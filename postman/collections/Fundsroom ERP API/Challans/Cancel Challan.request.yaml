$kind: http-request
name: Cancel Challan
method: POST
url: '{{baseUrl}}/api/challans/{{challanId}}/cancel'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
